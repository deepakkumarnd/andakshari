<nav class="fixed top-0 left-0 right-0 z-50 bg-white px-5 py-3">
  <div class="flex justify-between items-center">
    <div class="w-24"></div>
    <%= link_to root_path, class: "inline-flex flex-col leading-tight" do %>
      <span class="text-4xl font-bold text-primary font-logo">അന്താക്ഷരി</span>
    <% end %>
    <div class="flex items-center gap-3 text-sm w-24 justify-end">
      <% if user_signed_in? %>
        <%= button_to "Sign out", destroy_user_session_path, method: :delete,
            class: "rounded-md px-3 py-1.5 bg-gray-200 hover:bg-gray-300 text-gray-700 font-medium cursor-pointer" %>
      <% else %>
        <%= link_to "Sign in", new_user_session_path,
            class: "rounded-md px-3 py-1.5 bg-gray-700 hover:bg-gray-600 text-white font-medium" %>
      <% end %>
    </div>
  </div>
</nav>
